# Tint2 package

This package directory is the monorepo home for the local `tint2` packaging
used by the Openbox plus tint2 profile.

Unlike the Fedora-derived packages, this one starts from the repo-local
`tint2.spec` and source download logic.
