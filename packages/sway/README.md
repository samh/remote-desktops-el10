# Sway package

This package directory is the monorepo home for the local `sway` packaging
used by the Sway profile.

Pinned upstream source details live in `versions.env`, and the shared package
builder consumes the spec from `distgit/sway.spec`.
