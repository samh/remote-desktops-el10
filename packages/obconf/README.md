# Obconf package

This package directory is the local monorepo home for the Fedora-derived
`obconf` RPM packaging.

`distgit/` is refreshed from Fedora DistGit with
`scripts/update-package-from-fedora.sh obconf`.
